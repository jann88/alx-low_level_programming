#include "main.h"
/**
 * mul - multiplies two digits
 * @a: first integer to be tested
 * @b: second integer to be tested
 * @result: total after multiplication
 * Return: 0 
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
