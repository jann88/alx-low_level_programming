#include "main.h"
/**
 * mul - multiplies two digits
 * @a: first integer to be tested
 * @b: second integer to be tested
 * Return: 0 (success)
 */
int mul(int a, int b)
{
	int result = a * b;
	return (result);
}
