#include "main.h"
/**
 *_abs - computes absolute value of an integer
 *@n: is the value to be checked and computed
 *Return: absolute value of an integer
 */
int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}

